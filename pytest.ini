[pytest]
markers =
    unit: fast unit tests for a single module
    regression: fixture-based golden output checks
    integration: stage handoff checks on temp data dirs
testpaths = tests
pythonpath = .
